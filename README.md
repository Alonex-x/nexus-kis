# KIS — Keyboard Input Statistics

Personal productivity tool for Linux that tracks keyboard activity. Counts keystrokes in real time and saves daily summaries to a local SQLite database.

## Build

```bash
mkdir build && cd build
cmake ..
make

Usage

./kis
